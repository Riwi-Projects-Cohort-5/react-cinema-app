TASK-4 EDGE CASE QA VERIFICATION
=================================

1. Long Title & Long Synopsis Readability:
- Title constrained by `max-w-[600px]` with `break-words` / fluid wrap and `tracking-tight`.
- Synopsis constrained by `max-w-[460px]` with `text-text-secondary/85` and `leading-relaxed`.
- Left-to-right gradient overlay (`bg-gradient-to-r from-background/88 via-background/52 to-transparent`) ensures text contrast against bright or busy backdrops meets WCAG 2.2 AA (≥ 4.5:1 ratio).

2. Reduced-Motion Handling:
- Carousel track uses `motion-reduce:transition-none` alongside `transition-transform duration-500 ease-in-out`.
- Global CSS rule in `src/styles/main.css` resets `animation-duration: 0.01ms !important` and `transition-duration: 0.01ms !important` under `@media (prefers-reduced-motion: reduce)`.
- Progress strip fill percentage updates directly without CSS transition jitter.

3. Responsive Breakpoints Verification:
- Mobile (<768px): full height min-h-[600px] / 100vw, vertical flow, single slide viewport.
- Tablet (768px-1023px): h-[430px], side peek lateral preview.
- Desktop (≥1024px / 1440px): h-[520px], proportional slide width (67.8%), lateral peeks, sharp contrast.
