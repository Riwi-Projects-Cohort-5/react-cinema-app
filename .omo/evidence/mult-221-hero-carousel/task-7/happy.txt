# Hero Data States Geometry & Integration Summary (Task 7)

## Section Geometry Contract
All Hero data states (loading skeleton, error fallback, empty fallback, and populated carousel) strictly follow the full section geometry contract:
- Classes: `w-full h-[520px] md:h-[430px] min-h-[600px] md:min-h-0 bg-surface-variant`
- Outer radius: NONE (no `rounded-[1.25rem]`)
- No card wrappers or constrained max-widths

## State Verifications:
1. Loading skeleton (`isPending=true`):
   - `role="status"`
   - `aria-label="Cargando películas"`
   - `data-testid="hero-skeleton"`
   - `className="w-full h-[520px] md:h-[430px] min-h-[600px] md:min-h-0 bg-surface-variant animate-[shimmer_1.5s_linear_infinite] ..."`
2. Populated Carousel:
   - Full section geometry with interactive slide track, navigation buttons, and indicator controls.
3. Home Route Integration:
   - `renderRouter.tsx` configures `QueryClientProvider`
   - `appRouter.test.tsx` validates root route renders `Hero` with mocked `useMovies`.

Test Suite Output:
- `src/features/movies/components/hero/Hero.test.tsx`: 11 passed
- `src/routes/appRouter.test.tsx`: 9 passed
