=== Hero Carousel Behavior & QA Stabilization Summary ===
- Hook useHeroCarousel verified:
  * Autoplay advances slide every 5000ms via setInterval
  * Progress updates smoothly on 100ms tick interval
  * pause() and resume() freeze and restore carousel advancement deterministically
  * goTo(index), next(), and prev() handle circular wraparound correctly
  * Unmounting cleanly unregisters all active intervals
- Visual QA & Test attributes added to Hero.tsx:
  * data-carousel-state attribute on root section element reflects "paused" vs "playing"
  * data-active-index attribute on #hero-carousel viewport reflects activeIndex
- Tests:
  * 10 useHeroCarousel unit tests passing
  * 10 Hero component unit tests passing
  * 147 total test suite tests passing
